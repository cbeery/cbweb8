<% content_for :title, "Concerts" %>

<div class="flex justify-between items-center mb-6">
  <h1 class="text-3xl font-bold text-gray-900">Concerts</h1>
  <%= link_to "New Concert", new_admin_concert_path, 
      class: "px-4 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-700 transition-colors",
      data: { turbo_frame: "_top" } %>
</div>

<div class="bg-white shadow overflow-hidden sm:rounded-lg">
  <table class="min-w-full divide-y divide-gray-200">
    <thead class="bg-gray-50">
      <tr>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Artists</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Venue</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
      </tr>
    </thead>
    <tbody class="bg-white divide-y divide-gray-200">
      <% @concerts.each do |concert| %>
        <tr>
          <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
            <%= concert.played_on&.strftime('%b %d, %Y') %>
          </td>
          <td class="px-6 py-4 text-sm text-gray-500">
            <%= concert.concert_artists.pluck(:name).join(', ') %>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
            <%= concert.concert_venue&.display_name %>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
            <%= link_to "View", admin_concert_path(concert), class: "text-indigo-600 hover:text-indigo-900 mr-3" %>
            <%= link_to "Edit", edit_admin_concert_path(concert), class: "text-indigo-600 hover:text-indigo-900 mr-3" %>
            <%= link_to "Delete", admin_concert_path(concert), 
                data: { 
                  turbo_method: :delete,
                  turbo_confirm: "Are you sure?"
                },
                class: "text-red-600 hover:text-red-900" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>