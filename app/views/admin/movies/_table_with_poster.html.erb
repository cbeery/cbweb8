<!-- app/views/admin/movies/_table_with_poster.html.erb -->
<div class="bg-white shadow overflow-hidden sm:rounded-lg">
  <table class="min-w-full divide-y divide-gray-200">
    <thead class="bg-gray-50">
      <tr>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
          Poster
        </th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
          Title
        </th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
          Year
        </th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
          Rating
        </th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
          Views
        </th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
          Rewatch
        </th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
          Location
        </th>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
          Last Viewed
        </th>
      </tr>
    </thead>
    <tbody class="bg-white divide-y divide-gray-200">
      <% movies.each do |movie| %>
        <tr class="hover:bg-gray-50">
          <td class="px-6 py-4 whitespace-nowrap">
            <% poster = movie.movie_posters.first %>
            <% if poster&.image&.attached? %>
              <%= image_tag poster.image,
                  class: "h-20 w-14 object-cover rounded",
                  alt: movie.title,
                  loading: "lazy" %>
            <% elsif poster&.url %>
              <%= image_tag poster.url,
                  class: "h-20 w-14 object-cover rounded",
                  alt: movie.title,
                  loading: "lazy" %>
            <% else %>
              <div class="h-20 w-14 bg-gray-200 rounded flex items-center justify-center">
                <svg class="w-8 h-8 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z" clip-rule="evenodd" />
                </svg>
              </div>
            <% end %>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
            <%= link_to movie.title, admin_movie_path(movie), class: "text-gray-900 hover:text-indigo-600" %>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
            <%= movie.year || "-" %>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
            <% if movie.rating %>
              <div class="flex items-center gap-1">
                <span class="text-yellow-500">★</span>
                <span><%= movie.rating %></span>
              </div>
            <% else %>
              <span class="text-gray-400">-</span>
            <% end %>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
            <%= pluralize(movie.viewings.count, 'view') %>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
            <% rewatch_count = movie.viewings.rewatches.count %>
            <% if rewatch_count > 0 %>
              <span class="text-green-600"><%= rewatch_count %> rewatches</span>
            <% else %>
              <span class="text-gray-400">-</span>
            <% end %>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
            <% last_viewing = movie.viewings.order(viewed_on: :desc).first %>
            <% if last_viewing %>
              <%= last_viewing.location&.capitalize || "-" %>
            <% else %>
              <span class="text-gray-400">-</span>
            <% end %>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
            <% last_viewed = movie.viewings.maximum(:viewed_on) %>
            <%= last_viewed ? last_viewed.strftime("%b %d, %Y") : "Never" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
