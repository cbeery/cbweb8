<!-- app/views/home/test2.html.erb -->
<div class="mb-6 flex flex-col sm:flex-row sm:items-center sm:justify-between">
  <div>
    <h2 class="text-2xl font-bold text-gray-900">Test Layout 2</h2>
    <p class="text-sm text-gray-500 mt-1">Masonry-style grid with movie cards</p>
  </div>
  <div class="mt-4 sm:mt-0 flex gap-3">
    <%= link_to "Test 1", home_test1_path, class: "text-gray-600 hover:text-gray-800 text-sm" %>
    <%= link_to "Test 3", home_test3_path, class: "text-gray-600 hover:text-gray-800 text-sm" %>
    <%= link_to "← Home", root_path, class: "text-indigo-600 hover:text-indigo-800 text-sm font-medium" %>
  </div>
</div>

<!-- Masonry Grid using CSS Grid with auto-rows -->
<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 auto-rows-[80px]">
  
  <% if @all_movies&.any? %>
    <% @all_movies.each_with_index do |movie, index| %>
      <% 
        # Vary the row spans to create different card heights
        row_spans = [
          'row-span-4',  # 320px
          'row-span-5',  # 400px
          'row-span-6',  # 480px
          'row-span-4',  # 320px
          'row-span-5',  # 400px
          'row-span-7',  # 560px
        ]
        row_span = row_spans[index % row_spans.length]
        
        # Different color schemes for variety
        card_colors = [
          'from-blue-50 to-indigo-50 border-blue-200',
          'from-purple-50 to-pink-50 border-purple-200',
          'from-green-50 to-emerald-50 border-green-200',
          'from-yellow-50 to-orange-50 border-yellow-200',
          'from-red-50 to-rose-50 border-red-200',
          'from-cyan-50 to-teal-50 border-cyan-200'
        ]
        card_color = card_colors[index % card_colors.length]
      %>
      
      <div class="<%= row_span %> bg-gradient-to-br <%= card_color %> rounded-xl shadow-sm border hover:shadow-lg transition-shadow duration-200 overflow-hidden flex flex-col">
        
        <!-- Movie Poster - flexes to fill available space -->
        <% poster = movie.movie_poster %>
        
        <div class="relative flex-1 bg-white/50 min-h-0">
          <% if poster&.image&.attached? %>
            <%= image_tag poster.image,
                alt: movie.title,
                class: "absolute inset-0 w-full h-full object-cover" %>
          <% else %>
            <div class="absolute inset-0 flex items-center justify-center bg-gray-100">
              <svg class="w-16 h-16 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z" clip-rule="evenodd"/>
              </svg>
            </div>
          <% end %>
          
          <!-- Rating overlay -->
          <% if movie.rating %>
            <div class="absolute top-2 right-2 bg-black/70 text-white px-2 py-1 rounded-md text-sm font-medium backdrop-blur-sm">
              ★ <%= movie.rating %>
            </div>
          <% end %>
          
          <!-- Watch order badge -->
          <div class="absolute top-2 left-2 bg-white/90 backdrop-blur-sm text-gray-700 w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold shadow-md">
            <%= index + 1 %>
          </div>
        </div>
        
        <!-- Movie details - fixed height section -->
        <div class="flex-shrink-0 p-4 bg-white/50">
          <h3 class="font-semibold text-gray-900 mb-1 line-clamp-1">
            <%= movie.title %>
          </h3>
          
          <div class="flex items-center justify-between mb-2">
            <% if movie.year %>
              <p class="text-sm text-gray-600"><%= movie.year %></p>
            <% end %>
            
            <% if movie.director.present? %>
              <p class="text-xs text-gray-500 truncate ml-2">
                <%= movie.director %>
              </p>
            <% end %>
          </div>
          
          <!-- Viewing info - compact -->
          <% recent_viewing = movie.viewings.order(viewed_on: :desc).first %>
          <% if recent_viewing %>
            <div class="flex items-center justify-between text-xs text-gray-500">
              <p class="flex items-center">
                <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                </svg>
                <%= recent_viewing.viewed_on.strftime("%b %d") %>
              </p>
              
              <div class="flex items-center gap-2">
                <% if movie.viewings.count > 1 %>
                  <span class="text-gray-400">
                    <%= movie.viewings.count %>×
                  </span>
                <% end %>
                
                <% if recent_viewing.rewatch %>
                  <span class="px-1.5 py-0.5 bg-white/70 rounded text-xs">↻</span>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="col-span-full">
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-8 text-center">
        <svg class="w-16 h-16 text-gray-400 mx-auto mb-4" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z" clip-rule="evenodd"/>
        </svg>
        <p class="text-gray-500">No movies to display</p>
      </div>
    </div>
  <% end %>
</div>

<!-- Float Action Button -->
<div class="fixed bottom-6 right-6 sm:bottom-8 sm:right-8">
  <div class="bg-indigo-600 hover:bg-indigo-700 text-white rounded-full shadow-lg p-4 cursor-pointer transition-colors">
    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
    </svg>
  </div>
</div>