<!DOCTYPE html>
<html class="h-full bg-gray-100">
  <head>
    <title>Admin - <%= content_for(:title) || "curt.beery.co" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%# javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= javascript_importmap_tags %>
    <!-- Inter font -->
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
  </head>

  <body class="h-full" data-controller="navigation"<% if ENV['SHOW_BREAKPOINTS'] == 'true' %> data-controller="breakpoint navigation"<% end %>>

    <%= render partial: 'shared/breakpoint_banner' if ENV['SHOW_BREAKPOINTS'] == 'true'%>

    <div class="min-h-full">
      <%= render partial: 'shared/header/admin_header' %>
      <%= render partial: 'shared/nav/admin_nav' %>
      <%= render partial: 'shared/flash' %>

      <!-- Main content wrapped in card -->
      <main>
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="overflow-hidden bg-white shadow-xl sm:rounded-lg">
            <div class="px-4 py-5 sm:p-6">
              <%= yield %>
            </div>
          </div>
        </div>
      </main>
    </div>

  </body>
</html>