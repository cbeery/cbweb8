<!DOCTYPE html>
<html class="h-full bg-gray-100">
  <head>
    <title>Admin - <%= content_for(:title) || "MyApp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%# stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <!-- # Inter font -->
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
  </head>

  <body class="h-full">
    <div class="min-h-full">
      <!-- Admin Navigation -->
      <nav class="bg-gray-800">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-16 items-center justify-between">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <%= link_to "Admin Panel", admin_root_path, class: "text-white font-bold text-xl" %>
              </div>
              <div class="ml-10 flex items-baseline space-x-4">
                <%= link_to "Dashboard", admin_root_path, 
                    class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "S3 Test", admin_test_s3_path, 
                    class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Job Test", admin_test_job_path, 
                    class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Jobs Monitor", mission_control_jobs.root_path, 
                    class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
              </div>
            </div>
            <div class="flex items-center space-x-4">
              <span class="text-gray-300">Hi, <%= current_user.display_name %></span>
              <%= link_to "Main Site", root_path, class: "text-gray-300 hover:text-white" %>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, 
                  class: "text-gray-300 hover:text-white" %>
            </div>
          </div>
        </div>
      </nav>

      <!-- Flash messages -->
      <% if notice.present? || alert.present? %>
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
          <% if notice.present? %>
            <div class="rounded-md bg-green-50 p-4 mb-4">
              <p class="text-sm font-medium text-green-800"><%= notice %></p>
            </div>
          <% end %>
          <% if alert.present? %>
            <div class="rounded-md bg-red-50 p-4">
              <p class="text-sm font-medium text-red-800"><%= alert %></p>
            </div>
          <% end %>
        </div>
      <% end %>

      <!-- Main content -->
      <main>
        <div class="mx-auto max-w-7xl py-6 sm:px-6 lg:px-8">
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>